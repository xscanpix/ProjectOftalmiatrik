package com.anaxivis.application;

import javafx.application.Application;

public abstract class IAxanivis extends Application {
    public static AxanivisApplication waitForStartUpTest() {
	try {
	    latch.await();
	} catch (InterruptedException e) {
	    e.printStackTrace();
	}
	return application;
    }

    public static void setStartUpTest(AxanivisApplication app) {
	application = app;
	latch.countDown();
    }

}
